# PetStore
Project ini merupakan automation testing untuk backend (API) menggunakan BDD (Cucumber) dan RestAssured. Pengujian difokuskan pada validasi berbagai endpoint API yang mengacu pada referensi dari Swagger Petstore.

Cara menjalankan:
1. Jalankan file CucumberRunner
